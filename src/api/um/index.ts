// import { api } from "@/api/axios";

// Api for Profile and UM
const um = {
  //   getUM: async (payload: GetUmDtoIn): Promise<ResponseDto<GetUmDtoOut>> =>
  //     await api.get(`/um/${payload.userId}`),
}

export default um
